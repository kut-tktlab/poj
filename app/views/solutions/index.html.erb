<table class="table">
  <tr>
    <th>#</th>
    <th>状態</th>
  </tr>
  <% @solutions.each do |s| %>
    <tr>
      <td><%= link_to s.id, solution_path(s) %></td>
      <td><%= s.status %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'New solution', new_solution_path %>
